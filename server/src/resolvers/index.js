const Query = require('./Query')
const Mutation = require('./Mutation')
const Card = require('./Card')
const Project = require('./Project')
const User = require('./User')

module.exports = { Query, Mutation, Card, Project, User }
